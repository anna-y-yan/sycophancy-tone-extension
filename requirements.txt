# For running generation and (optionally) inference compatible with sycophancy-eval
tqdm>=4.0.0
matplotlib>=3.5
numpy>=1.20

# For inference (optional)
# langchain-openai>=0.0.5
# langchain-anthropic>=0.0.5
